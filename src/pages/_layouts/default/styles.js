import styled from 'styled-components';

export const Wrapper = styled.body`
  max-width: auto;
  min-width: auto;
  min-height: 800px;
  max-height: 2500px;
  width: auto;
  background: linear-gradient(#ff0000, #000099);
`;
